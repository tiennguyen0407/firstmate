FROM python:3.11-slim

WORKDIR /app

COPY memory_agent/requirements.txt ./
RUN pip install --no-cache-dir -r requirements.txt

COPY memory_agent/ ./memory_agent/

ENV PYTHONPATH=/app
EXPOSE 8080

CMD ["python", "-m", "uvicorn", "memory_agent.main:app", "--host", "0.0.0.0", "--port", "8080"]
